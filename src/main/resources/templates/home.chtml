<html>
  <body>
    <h1>Hello, {{ name }}</h1>
    <p>There are {{ count }} items.</p>

    {% if items %}
      <ul>
      {% for it in items %}
        <li>{{ it }}</li>
      {% endfor %}
      </ul>
    {% endif %}
  </body>
</html>
